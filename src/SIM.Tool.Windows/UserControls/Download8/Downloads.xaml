<UserControl x:Class="SIM.Tool.Windows.UserControls.Download8.Downloads" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="205" d:DesignWidth="443" Loaded="UserControlLoaded">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="36px" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0" TextAlignment="Left" TextWrapping="Wrap" Padding="0" Margin="0">
			* Sitecore 9+ versions should be downloaded manually from the <Hyperlink Click="OpenDownloadsPage">dev.sitecore.net</Hyperlink> site and placed to SIM Local Repository.
		</TextBlock>
		<ListBox Grid.Row="1" IsManipulationEnabled="False" Name="filePackages" SelectionChanged="ModuleSelected" Padding="0,5,0,0">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<CheckBox Content="{Binding}" IsChecked="{Binding Path=IsChecked}" IsEnabled="{Binding Path=IsEnabled}" Margin="4 0 4 10" Name="ololo" VerticalAlignment="Center" />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</UserControl>
