<UserControl x:Class="SIM.Tool.Windows.UserControls.MultipleDeletion.SelectSitecore9Environments"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="205" d:DesignWidth="443" Padding="0">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="180"></RowDefinition>
			<RowDefinition Height="25"></RowDefinition>
		</Grid.RowDefinitions>
		<ListBox Padding="0,5,0,0" Margin="0 -7 0 0" Name="Environments" ItemsSource="{Binding}" Grid.Row="0">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<CheckBox Content="{Binding Path=SitecoreEnvironment.Name}" IsChecked="False" Margin="4 0 4 10" VerticalAlignment="Center" ScrollViewer.VerticalScrollBarVisibility="Visible" Checked="OnChecked" Unchecked="OnUnchecked"/>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
		<TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Text="Scripts only:" Height="16" Margin="0,10,0,0" Grid.Row="1" />
		<CheckBox Name="ScriptsOnly" VerticalAlignment="Center" Height="15" Margin="70,10,0,0" Grid.Row="1" />
	</Grid>
</UserControl>