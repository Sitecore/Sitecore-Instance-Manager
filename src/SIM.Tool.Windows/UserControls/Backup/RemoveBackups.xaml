<UserControl x:Class="SIM.Tool.Windows.UserControls.Backup.RemoveBackups" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="205" d:DesignWidth="443">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="20"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"></RowDefinition>
            <RowDefinition Height="145"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
        </Grid.RowDefinitions>

        <ListBox Padding="0,5,0,0" Margin="0,-1,0,15" Name="Backups" ItemsSource="{Binding}" Grid.ColumnSpan="2" Grid.RowSpan="3">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Path=FolderPath}" IsChecked="{Binding Path=IsChecked}" Margin="4 0 4 10" VerticalAlignment="Center" ScrollViewer.VerticalScrollBarVisibility="Visible"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock HorizontalAlignment="Left" Margin="1,153,0,1" Width="98" Grid.RowSpan="2" Grid.Row="1">
            <Hyperlink Name="SelectAllHyperlink" Click="SelectAllHyperlinkClick">Select all</Hyperlink> 
            <Hyperlink Name="NoneHyperlink" Click="NoneHyperlinkClick">None</Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>